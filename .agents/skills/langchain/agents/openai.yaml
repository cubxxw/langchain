interface:
  display_name: "Langchain"
  short_description: "Repo-specific patterns and workflows for langchain"
  default_prompt: "Use the langchain repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true